### Implementation
- Use a stack that stores a pair of the value and current min, pair.first is the value, and pair.second is the min among the number inserted previously
- top() -> return stack.top.first
- getMin() -> return stack.top.second
​
### Complexity
- O(1) for each action
- O(n) in extra space
​
​